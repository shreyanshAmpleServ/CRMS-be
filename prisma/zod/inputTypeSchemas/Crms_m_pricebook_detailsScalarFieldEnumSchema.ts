import { z } from 'zod';

export const Crms_m_pricebook_detailsScalarFieldEnumSchema = z.enum(['id','parent_id','from_price','to_price','discount_per']);

export default Crms_m_pricebook_detailsScalarFieldEnumSchema;
