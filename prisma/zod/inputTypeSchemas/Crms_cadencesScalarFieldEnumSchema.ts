import { z } from 'zod';

export const Crms_cadencesScalarFieldEnumSchema = z.enum(['id','name','description','is_active','createdDate','updatedDate','createdBy','updatedBy','logInst']);

export default Crms_cadencesScalarFieldEnumSchema;
