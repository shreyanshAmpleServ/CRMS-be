import { z } from 'zod';

export const Crms_m_prioritiesScalarFieldEnumSchema = z.enum(['id','plevel','is_active','createdate','updatedate','createdby','updatedby','log_inst']);

export default Crms_m_prioritiesScalarFieldEnumSchema;
