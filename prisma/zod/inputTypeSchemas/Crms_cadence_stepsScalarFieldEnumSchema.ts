import { z } from 'zod';

export const Crms_cadence_stepsScalarFieldEnumSchema = z.enum(['id','cadence_id','step_order','action_type','template_id','description','wait_days']);

export default Crms_cadence_stepsScalarFieldEnumSchema;
