import { z } from 'zod';

export const Crms_cadence_assignmentsScalarFieldEnumSchema = z.enum(['id','cadence_id','lead_or_customer_id','current_step_id','next_action_date','status']);

export default Crms_cadence_assignmentsScalarFieldEnumSchema;
