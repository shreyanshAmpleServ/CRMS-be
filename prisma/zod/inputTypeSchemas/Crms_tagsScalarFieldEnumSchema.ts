import { z } from 'zod';

export const Crms_tagsScalarFieldEnumSchema = z.enum(['id','name','description','tagtype','is_active','createdate','updatedate','createdby','updatedby','log_inst']);

export default Crms_tagsScalarFieldEnumSchema;
