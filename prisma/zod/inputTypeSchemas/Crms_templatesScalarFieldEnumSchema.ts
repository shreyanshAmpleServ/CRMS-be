import { z } from 'zod';

export const Crms_templatesScalarFieldEnumSchema = z.enum(['id','name','type','content','createdDate','updatedDate','createdBy','updatedBy','logInst']);

export default Crms_templatesScalarFieldEnumSchema;
