import { z } from 'zod';

export const Crms_m_roleScalarFieldEnumSchema = z.enum(['id','role_name','is_active','log_inst','createdate','updatedate','createdby','updatedby']);

export default Crms_m_roleScalarFieldEnumSchema;
