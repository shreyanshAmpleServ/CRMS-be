import { z } from 'zod';

export const Crms_m_pricebookScalarFieldEnumSchema = z.enum(['id','name','model','is_active','description','effectivate_from','effectivate_to','createdate','createdby','updatedate','updatedby','log_inst']);

export default Crms_m_pricebookScalarFieldEnumSchema;
