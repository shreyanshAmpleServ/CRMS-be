import { z } from 'zod';

export const Crms_m_lead_statusScalarFieldEnumSchema = z.enum(['id','name','description','is_active','createdate','updatedate','createdby','updatedby','log_inst']);

export default Crms_m_lead_statusScalarFieldEnumSchema;
